import { Eye, EyeOff, ImagePlus, Pencil, Plus, Trash2 } from 'lucide-react'
import { useState } from 'react'
import { toast } from 'sonner'
import { PageHeader } from '../../components/admin/PageHeader'
import { ConfirmModal, EmptyState, ErrorState, LoadingState, Modal } from '../../components/ui'
import { useSupabaseList } from '../../hooks/useSupabaseList'
import { supabase } from '../../lib/supabase'
import type { Portfolio } from '../../types'
export function CatalogPage(){const {data,loading,error,reload}=useSupabaseList<Portfolio>('portfolios','*','sort_order');const [edit,setEdit]=useState<Partial<Portfolio>|null>(null);const [remove,setRemove]=useState<Portfolio|null>(null);const toggle=async(x:Portfolio)=>{await supabase.from('portfolios').update({is_active:!x.is_active}).eq('id',x.id);await reload()};const del=async()=>{if(!remove)return;const {error:e}=await supabase.from('portfolios').delete().eq('id',remove.id);if(e)toast.error(e.message);else toast.success('Katalog dihapus');setRemove(null);await reload()};return <><PageHeader title="Katalog" description="Kelola hasil jahitan yang tampil di landing page." action={<button className="btn-primary" onClick={()=>setEdit({title:'',description:'',category:'',image_url:'',is_active:true,sort_order:data.length+1})}><Plus size={18}/> Tambah karya</button>}/>{loading?<LoadingState/>:error?<ErrorState message={error} retry={reload}/>:data.length===0?<EmptyState title="Katalog masih kosong"/>:<div className="grid gap-5 sm:grid-cols-2 xl:grid-cols-3">{data.map(x=><article key={x.id} className="card overflow-hidden"><div className="relative aspect-[16/10] bg-sage-50"><img src={x.image_url} alt={x.title} className="h-full w-full object-cover"/><span className={`absolute left-3 top-3 rounded-full px-3 py-1 text-[10px] font-bold ${x.is_active?'bg-emerald-500 text-white':'bg-slate-800 text-white'}`}>{x.is_active?'Tampil':'Disembunyikan'}</span></div><div className="p-4 sm:p-5"><p className="text-xs font-bold uppercase tracking-widest text-sage-600">{x.category||'Umum'}</p><h2 className="mt-2 truncate text-lg font-bold text-ink">{x.title}</h2><p className="mt-2 line-clamp-2 min-h-10 text-sm leading-6 text-slate-500">{x.description||'Belum ada deskripsi karya.'}</p><div className="mt-4 flex justify-end gap-1 border-t border-sage-100 pt-3"><button title="Tampilkan/sembunyikan" className="rounded-lg p-2 text-slate-500 transition hover:bg-slate-50" onClick={()=>void toggle(x)}>{x.is_active?<EyeOff size={18}/>:<Eye size={18}/>}</button><button title="Edit karya" className="rounded-lg p-2 text-sage-700 transition hover:bg-sage-50" onClick={()=>setEdit(x)}><Pencil size={18}/></button><button title="Hapus karya" className="rounded-lg p-2 text-red-500 transition hover:bg-red-50" onClick={()=>setRemove(x)}><Trash2 size={18}/></button></div></div></article>)}</div>}{edit&&<CatalogModal value={edit} onClose={()=>setEdit(null)} onSaved={async()=>{setEdit(null);await reload()}}/>}<ConfirmModal open={!!remove} title="Hapus karya?" description="Karya akan dihapus permanen dari katalog." danger confirmLabel="Hapus" onClose={()=>setRemove(null)} onConfirm={()=>void del()}/></>}
function CatalogModal({value,onClose,onSaved}:{value:Partial<Portfolio>;onClose:()=>void;onSaved:()=>void}){const [form,setForm]=useState(value);const [file,setFile]=useState<File|null>(null);const [busy,setBusy]=useState(false);const save=async()=>{if(!form.title){toast.error('Judul wajib diisi');return}setBusy(true);let image=form.image_url||'';if(file){const path=`catalog/${crypto.randomUUID()}.${file.name.split('.').pop()}`;const up=await supabase.storage.from('portfolio').upload(path,file);if(up.error){toast.error(up.error.message);setBusy(false);return}image=supabase.storage.from('portfolio').getPublicUrl(path).data.publicUrl}const payload={title:form.title,description:form.description||null,category:form.category||null,image_url:image,is_active:form.is_active??true,sort_order:Number(form.sort_order)||0};const {error}=form.id?await supabase.from('portfolios').update(payload).eq('id',form.id):await supabase.from('portfolios').insert(payload);setBusy(false);if(error)toast.error(error.message);else{toast.success('Katalog disimpan');onSaved()}};return <Modal open title={form.id?'Edit karya':'Tambah karya'} onClose={onClose}><div className="space-y-4"><label><span className="mb-2 block text-xs font-bold">Judul</span><input className="input" value={form.title??''} onChange={e=>setForm({...form,title:e.target.value})}/></label><div className="grid grid-cols-2 gap-3"><label><span className="mb-2 block text-xs font-bold">Kategori</span><input className="input" value={form.category??''} onChange={e=>setForm({...form,category:e.target.value})}/></label><label><span className="mb-2 block text-xs font-bold">Urutan</span><input type="number" className="input" value={form.sort_order??0} onChange={e=>setForm({...form,sort_order:Number(e.target.value)})}/></label></div><label><span className="mb-2 block text-xs font-bold">Deskripsi</span><textarea className="input" value={form.description??''} onChange={e=>setForm({...form,description:e.target.value})}/></label><label className="flex cursor-pointer items-center gap-3 rounded-2xl border border-dashed border-sage-300 p-4 text-sm text-sage-700"><ImagePlus/> {file?.name||'Unggah foto katalog'}<input type="file" accept="image/*" className="sr-only" onChange={e=>setFile(e.target.files?.[0]??null)}/></label>{form.image_url&&!file&&<img src={form.image_url} className="h-32 w-full rounded-2xl object-cover"/>}<label className="flex items-center gap-3 text-sm"><input type="checkbox" checked={form.is_active??true} onChange={e=>setForm({...form,is_active:e.target.checked})}/> Tampilkan di landing page</label><button className="btn-primary w-full" onClick={()=>void save()} disabled={busy}>{busy?'Menyimpan...':'Simpan karya'}</button></div></Modal>}
