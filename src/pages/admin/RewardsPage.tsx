import { CheckCircle2, Gift, Pencil, Plus, Trash2 } from 'lucide-react'
import { useState } from 'react'
import { toast } from 'sonner'
import { PageHeader } from '../../components/admin/PageHeader'
import { ConfirmModal, EmptyState, ErrorState, LoadingState, Modal } from '../../components/ui'
import { useSupabaseList } from '../../hooks/useSupabaseList'
import { supabase } from '../../lib/supabase'
import type { Customer, Reward } from '../../types'
export function RewardsPage(){const {data,loading,error,reload}=useSupabaseList<Reward>('rewards','*');const customers=useSupabaseList<Customer>('customers','*');const [edit,setEdit]=useState<Partial<Reward>|null>(null);const [redeem,setRedeem]=useState<{customer:string;reward:string}|null>(null);const [remove,setRemove]=useState<Reward|null>(null);const redemption=async()=>{if(!redeem?.customer||!redeem.reward){toast.error('Pilih pelanggan dan reward');return}const {error:e}=await supabase.rpc('redeem_customer_reward',{p_customer_id:redeem.customer,p_reward_id:redeem.reward});if(e)toast.error(e.message);else{toast.success('Reward berhasil digunakan');setRedeem(null);await customers.reload()}};const del=async()=>{if(remove)await supabase.from('rewards').delete().eq('id',remove.id);setRemove(null);await reload()};return <><PageHeader title="Reward" description="Kelola pilihan reward dan catat penggunaannya." action={<div className="flex flex-wrap gap-2"><button className="btn-secondary" onClick={()=>setRedeem({customer:'',reward:''})}><CheckCircle2 size={18}/> Gunakan reward</button><button className="btn-primary" onClick={()=>setEdit({name:'',description:'',stamp_cost:5,stock:null,is_active:true})}><Plus size={18}/> Tambah reward</button></div>}/>{loading?<LoadingState/>:error?<ErrorState message={error} retry={reload}/>:data.length===0?<EmptyState title="Belum ada reward"/>:<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{data.map(x=><article key={x.id} className="card p-5"><span className="grid size-12 place-items-center rounded-2xl bg-amber-50 text-amber-600"><Gift/></span><div className="mt-5 flex items-start justify-between gap-3"><div><h2 className="font-bold">{x.name}</h2><p className="mt-2 min-h-10 text-sm text-slate-500">{x.description}</p></div><span className={`rounded-full px-3 py-1 text-[10px] font-bold ${x.is_active?'bg-emerald-50 text-emerald-700':'bg-slate-100 text-slate-500'}`}>{x.is_active?'Aktif':'Nonaktif'}</span></div><div className="mt-5 flex items-center justify-between border-t border-sage-100 pt-3"><p className="text-xs"><b>{x.stamp_cost}</b> stempel · Stok {x.stock??'∞'}</p><div><button className="p-2 text-sage-700" onClick={()=>setEdit(x)}><Pencil size={17}/></button><button className="p-2 text-red-500" onClick={()=>setRemove(x)}><Trash2 size={17}/></button></div></div></article>)}</div>}{edit&&<RewardModal value={edit} onClose={()=>setEdit(null)} onSaved={async()=>{setEdit(null);await reload()}}/>}<Modal open={!!redeem} title="Gunakan reward" onClose={()=>setRedeem(null)}><div className="space-y-4"><label><span className="mb-2 block text-xs font-bold">Pelanggan dengan reward</span><select className="input" value={redeem?.customer} onChange={e=>setRedeem({...redeem!,customer:e.target.value})}><option value="">Pilih pelanggan</option>{customers.data.filter(x=>x.available_rewards>0).map(x=><option key={x.id} value={x.id}>{x.name} ({x.available_rewards} tersedia)</option>)}</select></label><label><span className="mb-2 block text-xs font-bold">Reward</span><select className="input" value={redeem?.reward} onChange={e=>setRedeem({...redeem!,reward:e.target.value})}><option value="">Pilih reward</option>{data.filter(x=>x.is_active&&(x.stock===null||x.stock>0)).map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><p className="rounded-xl bg-amber-50 p-3 text-xs leading-5 text-amber-800">Stempel aktif pelanggan akan kembali menjadi nol. Riwayat stempel tetap tersimpan.</p><button className="btn-primary w-full" onClick={()=>void redemption()}>Konfirmasi penggunaan</button></div></Modal><ConfirmModal open={!!remove} title="Hapus reward?" description="Reward yang sudah memiliki riwayat penggunaan sebaiknya dinonaktifkan, bukan dihapus." danger confirmLabel="Hapus" onClose={()=>setRemove(null)} onConfirm={()=>void del()}/></>}
function RewardModal({value,onClose,onSaved}:{value:Partial<Reward>;onClose:()=>void;onSaved:()=>void}){const [f,setF]=useState(value);const save=async()=>{if(!f.name)return;const payload={name:f.name,description:f.description||null,stamp_cost:Number(f.stamp_cost)||5,stock:f.stock===null?null:Number(f.stock),is_active:f.is_active??true};const {error}=f.id?await supabase.from('rewards').update(payload).eq('id',f.id):await supabase.from('rewards').insert(payload);if(error)toast.error(error.message);else{toast.success('Reward disimpan');onSaved()}};return <Modal open title={f.id?'Edit reward':'Tambah reward'} onClose={onClose}><div className="space-y-4"><label><span className="mb-2 block text-xs font-bold">Nama reward</span><input className="input" value={f.name??''} onChange={e=>setF({...f,name:e.target.value})}/></label><label><span className="mb-2 block text-xs font-bold">Deskripsi</span><textarea className="input" value={f.description??''} onChange={e=>setF({...f,description:e.target.value})}/></label><div className="grid grid-cols-2 gap-3"><label><span className="mb-2 block text-xs font-bold">Biaya stempel</span><input type="number" className="input" value={f.stamp_cost??5} onChange={e=>setF({...f,stamp_cost:Number(e.target.value)})}/></label><label><span className="mb-2 block text-xs font-bold">Stok (kosong = ∞)</span><input type="number" className="input" value={f.stock??''} onChange={e=>setF({...f,stock:e.target.value===''?null:Number(e.target.value)})}/></label></div><label className="flex gap-3 text-sm"><input type="checkbox" checked={f.is_active??true} onChange={e=>setF({...f,is_active:e.target.checked})}/> Reward aktif</label><button className="btn-primary w-full" onClick={()=>void save()}>Simpan reward</button></div></Modal>}
