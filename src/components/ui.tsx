import { AlertTriangle, Inbox, LoaderCircle, X } from 'lucide-react'
import type { ReactNode } from 'react'

export function LoadingState({ label='Memuat data...' }: { label?:string }) { return <div className="flex min-h-48 flex-col items-center justify-center gap-3 text-sage-600"><LoaderCircle className="animate-spin"/><p className="text-sm">{label}</p></div> }
export function EmptyState({ title='Belum ada data', description='Data yang ditambahkan akan muncul di sini.' }: { title?:string; description?:string }) { return <div className="flex min-h-48 flex-col items-center justify-center gap-2 rounded-2xl border border-dashed border-sage-200 p-6 text-center"><Inbox className="text-sage-400"/><b>{title}</b><p className="text-sm text-slate-500">{description}</p></div> }
export function ErrorState({ message, retry }: { message:string; retry?:()=>void }) { return <div className="flex min-h-40 flex-col items-center justify-center gap-3 rounded-2xl bg-red-50 p-6 text-center text-red-700"><AlertTriangle/><p>{message}</p>{retry&&<button className="btn-secondary" onClick={retry}>Coba lagi</button>}</div> }
export function Modal({ open,title,children,onClose }: { open:boolean; title:string; children:ReactNode; onClose:()=>void }) { if(!open)return null; return <div className="fixed inset-0 z-[100] grid place-items-center bg-ink/40 p-4 backdrop-blur-sm" onMouseDown={onClose}><div className="card max-h-[90vh] w-full max-w-lg overflow-y-auto p-5" onMouseDown={e=>e.stopPropagation()}><div className="mb-5 flex items-center justify-between"><h2 className="text-xl font-bold">{title}</h2><button aria-label="Tutup" onClick={onClose} className="rounded-full p-2 hover:bg-sage-50"><X size={20}/></button></div>{children}</div></div> }
export function ConfirmModal({ open,title,description,confirmLabel='Ya, lanjutkan',danger=false,onConfirm,onClose }: {open:boolean;title:string;description:string;confirmLabel?:string;danger?:boolean;onConfirm:()=>void;onClose:()=>void}) { return <Modal open={open} title={title} onClose={onClose}><p className="mb-6 text-sm leading-6 text-slate-600">{description}</p><div className="flex justify-end gap-2"><button className="btn-secondary" onClick={onClose}>Batal</button><button className={danger?'rounded-full bg-red-600 px-5 py-3 font-bold text-white':'btn-primary'} onClick={onConfirm}>{confirmLabel}</button></div></Modal> }
